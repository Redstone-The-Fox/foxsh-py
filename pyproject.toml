[tool.poetry]
name = "foxsh-py"
version = "0.1.5"
description = "FoxShell for Debian"
authors = ["Arcticfox"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
